# W.A.S.P v26 – Dialog Alignment & Action Label Polish

- Centered the custom question/information glyph inside its circular badge using a StackPane.
- Added a small optical vertical adjustment to compensate for font glyph metrics.
- Backup Reminder now uses the information `i` badge rather than the confirmation `?` badge.
- `Continue Without Backup` is given an explicit wider button allocation so the full label remains visible.
- Preserved the v25 W.A.S.P dialog styling and action semantics.

### v29 update
- Backup Reminder is explicitly detected by dialog title before generic confirmation styling so its badge remains informational `i` rather than `?`.
